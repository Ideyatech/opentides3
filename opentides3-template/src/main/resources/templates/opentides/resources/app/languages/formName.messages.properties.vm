#
# This is the message property file for ${bean.className}.
# The contents of this file are used for globalization purposes.
# Scaffold generated by opentides3 on ${dateToday}. 
# @author opentides
#

#basic labels used for crud pages
label.${bean.formName} = ${bean.displayName}
label.${bean.formName}.add = Add ${bean.displayName}
label.${bean.formName}.update = Update ${bean.displayName}

#labels used for the fields of ${bean.displayName}
#foreach( ${field} in ${bean.fields} )
#if ( ${field.getAttribute("label")} != "")
label.${bean.formName}.${field.getLabelCode()} = ${field.getLabel()}
#end
#end